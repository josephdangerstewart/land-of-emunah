name: Build

on:
  push:
    branches:
      - 'master'

jobs:
  build_and_deploy:
    uses: josephdangerstewart/configuration/.github/workflows/deploy-nextjs-app.yaml@master
    with:
      deployment_host: 64.227.48.74
      node_version: 16.x
      port: 3000
    secrets:
      ssh_private_key: ${{ secrets.DEPLOYMENT_SSH_KEY }}
